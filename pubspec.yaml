name: skill_swiper
description: Lokale Tauschwirtschaft für Fähigkeiten — Skill-Tinder
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.8.0

  # State Management
  flutter_riverpod: ^2.6.0
  riverpod_annotation: ^2.6.0

  # Routing
  go_router: ^14.8.0

  # UI
  flutter_card_swiper: ^7.0.0
  cached_network_image: ^3.4.0

  # Utils
  geolocator: ^13.0.0
  geocoding: ^3.0.0
  image_picker: ^1.1.0
  image_cropper: ^8.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.6.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
